// https://codeforces.com/contest/1186/problem/A
#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main() {
  cin.tie(0);
  ios::sync_with_stdio(0);
  int n, m, k;
  cin >> n >> m >> k;
  cout << (n <= m && n <= k ? "Yes\n" : "No\n");;
}
