#include<bits/stdc++.h>
using namespace std;

int t,m,n;

main(){
    scanf("%d", &t);
    while(t--){
        scanf("%d %d", &n, &m);
        int arr[n][m];
        for(int i=0; i<n; i++)
            for(int j=0; j<m; j++)
                scanf("%d", &arr[i][j]);


    }
    return 0;
}
